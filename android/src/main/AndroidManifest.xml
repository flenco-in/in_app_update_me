<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />

  <!--
    REQUEST_INSTALL_PACKAGES is intentionally NOT declared here: merging it
    into every consumer app triggers Google Play policy review even for apps
    that only use Play in-app updates. Apps using direct APK installs must
    declare it in their own manifest (see README).
  -->
</manifest>
